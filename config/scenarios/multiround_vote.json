{
  "name": "multiround_vote",
  "__comment__": [
    "This file describes a *scenario* for the generic generator runner.",
    "Multi-round voting is just one scenario: it is expressed as an ordered list of ops.",
    "The model routing / sampling params / thresholds still live in config/llm_models.json."
  ],
  "steps": [
    {
      "op": "stage1_infer",
      "__comment__": "Input is the raw dataset JSONL path (file or directory).",
      "input": "${RAW_INPUT}"
    },
    {
      "op": "stage1_eval",
      "__comment__": "Use OUT_DIR as search root so it can discover stage1_infer/stage1_output artifacts.",
      "input": "${OUT_DIR}"
    },
    { "op": "stage2_infer", "input": "${OUT_DIR}" },
    { "op": "stage2_eval", "input": "${OUT_DIR}" },
    { "op": "stage3_infer", "input": "${OUT_DIR}" },
    { "op": "stage3_eval", "input": "${OUT_DIR}" },
    {
      "op": "result_rebuild",
      "__comment__": "Rebuild results from OUT_DIR; this op ignores RAW_INPUT.",
      "input": "${OUT_DIR}"
    }
  ]
}

